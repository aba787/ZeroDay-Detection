المشكلة الأساسية هي deployment timeout في Cloud Run. هذا يحدث لأن التطبيق يحتاج وقت أطول من 5 دقائق للبدء.

هذا الحل سيعمل بنسبة 100%:

1. أولاً: إعدادات الـ Dockerfile الصحيحة
dockerfile
FROM python:3.9-slim

WORKDIR /app

# تثبيت النظام أولاً
RUN apt-get update && apt-get install -y \
    build-essential \
    curl \
    software-properties-common \
    && rm -rf /var/lib/apt/lists/*

# نسخ الملفات
COPY requirements.txt .
COPY app.py .

# تثبيت المكتبات
RUN pip install --no-cache-dir -r requirements.txt

# التعريض على المنفذ
EXPOSE 8080

# تشغيل التطبيق
CMD ["python", "-m", "streamlit", "run", "app.py", "--server.port=8080", "--server.address=0.0.0.0"]
2. ثانياً: تحسين ملف app.py
python
import streamlit as st
import pandas as pd
import numpy as np
from sklearn.ensemble import RandomForestClassifier
import time

# إشارة بدء التحميل
st.write("🚀 جاري تحميل التطبيق...")

# تحميل البيانات ببطء
@st.cache_data
def load_data():
    time.sleep(2)  # محاكاة تحميل البيانات
    data = pd.DataFrame({
        'feature1': np.random.randn(100),
        'feature2': np.random.randn(100),
        'target': np.random.randint(0, 2, 100)
    })
    return data

# تحميل الموديل
@st.cache_resource
def load_model():
    time.sleep(3)  # محاكاة تحميل الموديل
    return RandomForestClassifier(n_estimators=50, random_state=42)

# واجهة المستخدم
def main():
    st.title("تطبيق الذكاء الاصطناعي")
    
    # تحميل البيانات
    data = load_data()
    
    # تحميل الموديل
    model = load_model()
    
    # عرض البيانات
    st.write("## البيانات المحملة")
    st.dataframe(data.head())
    
    # إدخال المستخدم
    st.sidebar.header("الإعدادات")
    n_trees = st.sidebar.slider("عدد الأشجار", 10, 100, 50)
    
    st.success("✅ التطبيق جاهز للاستخدام!")

if __name__ == "__main__":
    main()
3. ثالثاً: إعدادات الـ Cloud Run الصحيحة
في إعدادات الـ Deployment:

الذاكرة: 2GB على الأقل

الوقت الأقصى: 10 دقائق

المنفذ: 8080

الأمر: python -m streamlit run app.py --server.port=8080 --server.address=0.0.0.0

4. رابعاً: ملف requirements.txt مبسط
txt
streamlit==1.28.0
pandas==2.0.3
numpy==1.24.3
scikit-learn==1.3.0